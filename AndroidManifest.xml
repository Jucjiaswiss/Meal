<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.szu.zhaoyujia.meal">

    <application
        android:name=".Tools.MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".log_reg.LogActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".log_reg.RegisterActivity" ></activity>
        <activity android:name=".Discover.ClassifyActivity" ></activity>
        <activity android:name=".SearchActivity" ></activity>
        <activity android:name=".Me.LikeActicity" ></activity>
        <activity android:name=".Me.FollowActicity" ></activity>
        <activity android:name=".Discover.ClassifyItemActivity" ></activity>
        <activity android:name=".MealActivity" ></activity>
        <activity android:name=".Record.AddRecdActivity" ></activity>
        <activity android:name=".Recommend.SwipeRecmActivity" ></activity>
    </application>

    <!-- if you want to load images from the internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- if you want to load images from a file OR from the internet -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!--Bmob-->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!--动态申请-->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
</manifest>